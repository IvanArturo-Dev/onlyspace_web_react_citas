# Conflicto conocido de peer deps en devDependencies: @babel/core@8 vs ts-jest (pide @babel/core@7).
# No afecta runtime; permite que npm ci resuelva en CI/CD y Docker sin --legacy-peer-deps.
legacy-peer-deps=true
